var tall = [13, 2, 25, 4, 38];

var number = [];

function myFunction(tall){
    e.preventDefault();
    for(var i = 0; i < tall.length; i++){
        var tall = tall[i];
        if(tall[i] > 5){
            number.push(tall[i])
        }
        console.log(number);
    }
}
